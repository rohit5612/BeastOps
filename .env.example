# Copy to `.env` at repo root if you use root-level tooling.
# API reads `apps/api/.env` when you run the API from that folder.

DATABASE_URL=postgresql://beastops:beastops@localhost:5432/beastops?schema=public
